import { createTheme } from "@mantine/core";

export default createTheme({
  primaryColor: "orange",
  autoContrast: true,
  black: "#141414",
  defaultGradient: {
    from: "#f50",
    to: "#d6bb00",
    deg: 135,
  },
});
